FETCH SECTION DATA
    check fallback getSectionTakersByExamId

    result.rows not showing

    create exam again to check section post/update 
            === checked, this one still not working, which means:
                post/update is the problem, and not the incrementation in wtv (possible)

    console.log 1.Backend data from front
                2.Frontend data from back (not the variable value, but fetch from db)
    
    use key or array,,, basta row... probly exam_id to fetch the data. then yun lagay sa sa section

    render the section_name sa front, tutal for display lang... 
    SECTION_NAME not the per year, course, section...
    just the array of SECTION_NAME from db result
    
    how to get the values on checkbox?
        set values, useState (fetch from db) choices from sec/year/wtvr.