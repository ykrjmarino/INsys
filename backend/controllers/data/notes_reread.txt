✅ Use req.params when:
You're accessing one specific resource.

Why? The URL path clearly says what you're looking for.

Example:
  GET /api/users/1
  → req.params.id = 1
  Meaning: “Get me the user with ID 1.”

✅ Use req.query when:
You're filtering, searching, or passing optional things.

Why? You might pass multiple flexible filters.

Example:
  GET /api/users?role=student&active=true
  → req.query.role = "student"
  → req.query.active = "true"
  Meaning: “Get all active students.”


Viewing a single user? Use params.
Searching users by role/status/etc? Use query.


✅ Use if (result.rows.length === 0) {
        return res.status(404).json({ message: 'Exam not found' })
      }
when:
GET/UPDATE/DELETE

✅ Use RETURNING * when:
INSERT/UPDATE/DELETE


✅ Use ON DELETE CASCADE when:

The child data is meaningless without the parent.

Use it if:
You don’t need to keep orphan records

You always want related data wiped automatically

Example:
section_takers depends on examinations

questions depends on examinations

dalawa env mo bui:

backend:
# Server
PORT=3000

# Database (Postgres)
DB_USER=postgres
DB_PASSWORD=marchdb
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres

# JWT Secrets
JWT_ACCESS_SECRET=diko_alam_ilalagay_na_access_secret
JWT_REFRESH_SECRET=diko_alam_ilalagay_na_refresh_secret
JWT_ACCESS_EXPIRES_IN=15m   # example: 15 minutes

# Nodemailer (NDM)
NDM_USER=gunshogun333@gmail.com
NDM_PASSWORD=tndrpuojgthtbttb

frontend:
# Frontend (Vite)
VITE_API_BASE_URL=http://localhost:3000/api




sa deployment

back
# JWT Secrets
JWT_ACCESS_SECRET=diko_alam_ilalagay_na_access_secret
JWT_REFRESH_SECRET=diko_alam_ilalagay_na_refresh_secret
JWT_ACCESS_EXPIRES_IN=15m   # example: 15 minutes

# Nodemailer (NDM)
NDM_USER=gunshogun333@gmail.com
NDM_PASSWORD=tndrpuojgthtbttb

REDIS_URL=redis://default:smZGoC1cxEHZ2yiZKXfZYVhbwfchBk71@redis-17264.c89.us-east-1-3.ec2.redns.redis-cloud.com:17264

# DATABASE_URL=postgresql://insys_db_user:rhVS3mVxDcAdmFiqproE0hwwlBd7mx6v@dpg-d3j4bc8dl3ps73dkde5g-a/insys_db
DATABASE_URL=postgresql://insys_db_user:rhVS3mVxDcAdmFiqproE0hwwlBd7mx6v@dpg-d3j4bc8dl3ps73dkde5g-a.oregon-postgres.render.com/insys_db

SENDGRID_API_KEY=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

front
VITE_API_BASE_URL=https://insys-89dk.onrender.com/api
VITE_API_URL=https://insys-89dk.onrender.com
VITE_WS_URL=wss://insys-backend.onrender.com/ws
